//We specify what project name this is.
rootProject.name = 'PokemonWalking'

//We then specify the sub-projects here. This is the only place where Gradle has
//knowledge of which projects are child projects of the parent project.
include "common"
include "levelEditor"
include "game"